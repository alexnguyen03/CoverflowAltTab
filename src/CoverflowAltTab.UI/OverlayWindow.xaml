<Window x:Class="CoverflowAltTab.UI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CoverflowAltTab Overlay"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="True"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <Style x:Key="CoverflowSurfaceVisibilityStyle"
               TargetType="Grid">
            <Setter Property="Visibility"
                    Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UsesFreeformLayout}"
                             Value="True">
                    <Setter Property="Visibility"
                            Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ListSurfaceVisibilityStyle"
               TargetType="ItemsControl">
            <Setter Property="Visibility"
                    Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UsesFreeformLayout}"
                             Value="True">
                    <Setter Property="Visibility"
                            Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border Background="#D60B0F14"
            CornerRadius="12"
            Padding="24">
        <Grid>
            <Grid Width="{Binding StageWidth}"
                  Height="{Binding StageHeight}"
                  Style="{StaticResource CoverflowSurfaceVisibilityStyle}">
                <Rectangle HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Width="760"
                           Height="92"
                           Margin="0,0,0,12"
                           Opacity="0.18"
                           IsHitTestVisible="False">
                    <Rectangle.Fill>
                        <RadialGradientBrush GradientOrigin="0.5,0.45">
                            <GradientStop Color="#FFFFFFFF"
                                          Offset="0"/>
                            <GradientStop Color="#00FFFFFF"
                                          Offset="1"/>
                        </RadialGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <!-- Fixed 3-slot coverflow (left/center/right), positioned and animated
                     directly in OverlayWindow.xaml.cs via TranslateTransform + ScaleTransform,
                     the same technique as the CarouselDemo prototype. -->
                <Canvas HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Width="1"
                        Height="1"
                        IsHitTestVisible="False">

                    <Grid x:Name="LeftCard"
                          Width="400" Height="260"
                          Canvas.Left="-200" Canvas.Top="-130"
                          RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <TranslateTransform x:Name="LeftCardTranslate"/>
                                <ScaleTransform x:Name="LeftCardScale"/>
                            </TransformGroup>
                        </Grid.RenderTransform>
                    </Grid>

                    <Grid x:Name="CenterCard"
                          Width="400" Height="260"
                          Canvas.Left="-200" Canvas.Top="-130"
                          RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <TranslateTransform x:Name="CenterCardTranslate"/>
                                <ScaleTransform x:Name="CenterCardScale"/>
                            </TransformGroup>
                        </Grid.RenderTransform>
                    </Grid>

                    <Grid x:Name="RightCard"
                          Width="400" Height="260"
                          Canvas.Left="-200" Canvas.Top="-130"
                          RenderTransformOrigin="0.5,0.5">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <TranslateTransform x:Name="RightCardTranslate"/>
                                <ScaleTransform x:Name="RightCardScale"/>
                            </TransformGroup>
                        </Grid.RenderTransform>
                    </Grid>

                </Canvas>

                <Border HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Margin="0,0,0,20"
                        Padding="24,10"
                        Background="#CC141A20"
                        CornerRadius="20"
                        IsHitTestVisible="False"
                        Panel.ZIndex="1000">
                    <TextBlock Text="{Binding SelectedTitle}"
                               Foreground="#FFF4FBFF"
                               FontSize="16"
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="640"
                               HorizontalAlignment="Center"/>
                </Border>
            </Grid>

            <ItemsControl ItemsSource="{Binding Windows}"
                          Width="388"
                          Style="{StaticResource ListSurfaceVisibilityStyle}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8"
                                Padding="10"
                                CornerRadius="10">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background"
                                            Value="#22FFFFFF"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}"
                                                     Value="True">
                                            <Setter Property="Background"
                                                    Value="#FF0E639C"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding Title}"
                                       Foreground="White"
                                       FontSize="14"
                                       TextTrimming="CharacterEllipsis"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</Window>
